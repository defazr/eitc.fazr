# GPT 핸드오프 — 2026-04-18 (D-13 to 5/1)

## 오늘 완료한 작업 (세션 6 — 전환률 개선)

### 배경
- 4일치 Google Ads 데이터 기준 전환율 1.8% (목표 5%)
- CTR 8% 우수, 키워드 정확, 타겟 정확
- 문제 진단: 사이트 UX → 계산기까지 도달 못 함
- D-13 시즌 진입 직전 전환률 개선 시급

### 코드 작업 (Claude Code, 커밋 d654ba8)

| 작업 | 내용 |
|---|---|
| D-Day 배너 | 제목 아래 파란색 배너, `new Date("2026-05-01")` 기준 자동 계산 ("5월 1일 신청 시작 D-{n}") |
| 버튼 항상 활성화 | `disabled` 제거, 소득·재산 미입력 시 `alert()` + `scrollIntoView({ block: "center" })` |
| PC/모바일 분리 | PC: `hidden md:block` 기존 위치 / 모바일: `md:hidden fixed bottom-0` sticky footer |
| 기대감 문구 | 가구유형 카드 위 "입력 3개만 하면 30초 안에 내 예상 금액 확인" |
| 버튼 텍스트 | "예상 장려금 계산하기" → "지금 바로 내 금액 확인하기 →" |
| 하단 padding | wrapper에 `pb-24 md:pb-12` — sticky 버튼에 콘텐츠 안 가림 |
| Input id 추가 | `id="income"`, `id="asset"` — scrollIntoView 타겟 |

### 기술 결정 사항

1. **가구유형 검증 제외**: 기본값 `"single"` 있어서 미선택 상태 없음. 소득·재산 2개만 검증.
2. **alert 사용**: 토스트 라이브러리 없음. 신규 의존성 추가 안 함.
3. **카톡 버튼 충돌 없음**: 카톡 버튼은 `isCalculated` 후에만 노출, sticky footer와 겹치지 않음.

## 현재 상태

- **최신 커밋**: `d654ba8` (main, pushed)
- **빌드**: 통과
- **테스트**: 61개 전부 통과
- **변경 파일**: `src/app/calculator/page.tsx` (1개, +64 -11)

## 검증 체크리스트

- [ ] PC: D-Day 배너 표시 + 버튼 기존 위치에 활성화
- [ ] 모바일: D-Day 배너 + sticky footer 버튼 상시 노출
- [ ] 빈 입력 → 버튼 클릭 → alert "총급여액을 입력해 주세요" + 해당 필드로 스크롤
- [ ] 소득 입력, 재산 미입력 → 버튼 클릭 → alert "가구원 합산 재산을 입력해 주세요" + 재산 필드로 스크롤
- [ ] 모두 입력 후 계산 → 기존 결과 페이지 정상 (카톡/공유/AdSense 그대로)
- [ ] GA4 calculator_click + Google Ads conversion 정상 발화

## 다음 작업

### 즉시 (4/18 내)
- Vercel 배포 확인 (2~5분)
- 모바일/PC 실기기 테스트
- 네이버 데이터 수집 → PR-5 착수 결정

### PR-5 백로그 (미정, GSC/네이버 데이터 보고 결정)
필수 3개:
1. FAQ Q16 "미신청" (월 22,200)
2. FAQ Q17 "홈택스" (월 4,500+)
3. /eligibility 메타 수정 "대상자 확인" (월 4,400+)

선택 1개:
4. Update 신규 1편 "지급일 총정리" (월 5,000+)

### 전환율 모니터링
- 기준: 4/14~4/17 전환율 1.8%
- 목표: 5%+ (배포 후 48시간 관찰)
- 비교 기간: 4/18~4/20 vs 이전 4일
